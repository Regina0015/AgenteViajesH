# ── Sensible: credenciales — NUNCA se suben ──────────────────────
.env
.env.*
*.env
.env.example

# ── Pesado: dependencias y builds ─────────────────────────────────
node_modules/
web/dist/
web/.vite/

# ── Datos locales: BD de desarrollo y evidencias subidas ─────────
server/*.db
server/*.db-journal
server/*.db-wal
server/*.db-shm
server/uploads/*
!server/uploads/.gitkeep

# ── Ruido ─────────────────────────────────────────────────────────
*.log
npm-debug.log*
.DS_Store
Thumbs.db
